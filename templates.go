package main

// Templates are in here as constants (rather than files) so they are always available
// without hitting the file system. Some of these are call a lot.

const (
	HTMLWaitingForOthers = ""
)
